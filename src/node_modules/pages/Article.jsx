import React, {useEffect, useContext, useState} from 'react'
import useFirebase from 'hooks/useFirebase'
import useDeleteFirebase from 'hooks/useDeleteFirebase'
import {Link, Redirect} from 'react-router-dom'
import Loading from 'components/Loading'
import ErrorMessage from 'components/ErrorMessage'
import TagList from 'components/TagList'
import {CurrentUserContext} from 'contexts/currentUser'

const Article = ({match}) => {
  const slug = match.params.slug
  const [{response, error, isLoading}, dbGet, dispatch] = useFirebase()
  const [{response: deleteResponse}, doDeleteArticle] = useDeleteFirebase()
  const [currentUserState] = useContext(CurrentUserContext)
  const [isSuccessFullSubmit, setIsSuccessFullSubmit] = useState(false)

  const isAuthor = () => {
    if (!response || !currentUserState.isLoggedIn) return
    return response.posts[0][0].userUid === currentUserState.currentUser.uid
  }

  const deleteArticle = () => {
    doDeleteArticle({
      url: '/posts',
      id: slug
    })
  }

  useEffect(() => {
    dispatch({
      type: 'GET_ONE_POST',
      payload: slug
    })
    dbGet()

  }, [slug, dbGet])

  useEffect(() => {
    if (!deleteResponse) return
    setIsSuccessFullSubmit(true)
  }, [deleteResponse])

  if (isSuccessFullSubmit) {
    return <Redirect to={'/feed'}/>
  }

  let post
  if (response) {
    post = response.posts[0][0]
  }

  return (
    <div className={'article-page'}>
      <div className="banner">
        {!isLoading && response && (
          <div className={'container'}>
            <h1>{post.title}</h1>
            <div className={'article-meta'}>
              <Link to={`/profiles/${post.username}`}>
                <img src={post.image} alt=""/>
              </Link>
              <div className="info">
                <Link to={`/profiles/${post.username}`}>
                  {post.username}
                </Link>
                <span className="date">{new Date(post.createdAt).toDateString()}</span>
              </div>
              {isAuthor() && (
                <span>
                  <Link
                    className={'btn btn-outline-secondary btn-sm'}
                    to={`/articles/${post.id}/edit`}
                  ><i className={'ion-edit'}/>
                  Edit Article
                  </Link>
                  <button
                    className={'btn btn-outline-danger btn-sm'}
                    onClick={deleteArticle}
                  >
                    <i className={'ion-trash-a'}/>
                    Delete article
                  </button>
                </span>
              )}
            </div>
          </div>
        )}
      </div>
      <div className="container page">
        {isLoading && <Loading/>}
        {error && <ErrorMessage/>}
        {!isLoading && response && (
          <div className={'row article-content'}>
            <div className="col-xs-12">
              <div>
                <p>{post.description}</p>
              </div>
              <TagList tags={post.tagList}/>
            </div>
          </div>
        )}
        <hr/>
      </div>
    </div>
  )
}

export default Article
