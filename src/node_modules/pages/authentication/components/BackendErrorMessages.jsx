import React from 'react'

const BackendErrorMessages = ({backendErrors}) => {
  return (
    <ul className='error-messages'>
      {(<li key={backendErrors}>{backendErrors}</li>)}
    </ul>
  )
}

export default BackendErrorMessages
